% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_lexicon}
\alias{get_lexicon}
\title{Retrieve a sentiment lexicon by name}
\usage{
get_lexicon(lexicon)
}
\arguments{
\item{lexicon}{Character. One of the ten lexicon keys returned by
\code{\link{mixology_lexicon_names}}: \code{"inquirer"},
\code{"subjectivity"}, \code{"bing"}, \code{"nrc"}, \code{"afinn"},
\code{"loughran"}, \code{"covid"} (Mixology Covid Lexicon),
\code{"mixology"} (merged Mixology Lexicon),
\code{"covid_ft"} (fine-tuned Mixology Covid Lexicon), or
\code{"mixology_ft"} (fine-tuned Mixology Lexicon).}
}
\value{
A tibble. All lexicons include \code{word} (character) and
\code{sentiment} (one of \code{"positive"}, \code{"negative"}, or
\code{"ambiguous"}). The \code{"covid"}, \code{"mixology"},
\code{"covid_ft"}, and \code{"mixology_ft"} lexicons additionally include:
\describe{
  \item{weight}{Numeric. Log-normalised corpus frequency weight in the
    range 0.5--3.0. Used in weighted scoring via
    \code{\link{mixology_sentiment}}. New terms added during fine-tuning
    receive a default weight of 1.0.}
  \item{freq_corpus}{Integer. Raw token frequency in the reference
    corpus. New terms added during fine-tuning receive 0.}
}
}
\description{
Convenience accessor for all ten sentiment lexicons bundled in the
Mixology package, including two fine-tuned variants. Returns a tibble
with polarity assignments for each term.
}
\examples{
# Retrieve the Mixology Covid lexicon
get_lexicon("covid")

# Retrieve the fine-tuned Mixology lexicon
get_lexicon("mixology_ft")

# Retrieve the Bing Liu lexicon
get_lexicon("bing")

# Inspect positive terms sorted by corpus frequency
library(dplyr)
get_lexicon("covid") |>
  filter(sentiment == "positive") |>
  arrange(desc(freq_corpus)) |>
  head(20)
}
\seealso{
\code{\link{mixology_lexicon_names}}, \code{\link{mixology_sentiment}},
\code{\link{lexicon_conflicts}}, \code{\link{use_custom_lexicon}}
}

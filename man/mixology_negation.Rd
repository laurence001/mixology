% Generated by roxygen2: do not edit by hand
\name{mixology_negation}
\alias{mixology_negation}
\title{Apply negation handling to a tokenised table}
\usage{
mixology_negation(tokens_tbl, window = 3L, custom_negations = NULL)
}
\arguments{
\item{tokens_tbl}{A tibble as returned by \code{\link{mixology_tokenize}},
with columns \code{doc_id} (integer) and \code{token} (character).}

\item{window}{Integer. Number of tokens to flag after each negation marker.
Default \code{3L}.}

\item{custom_negations}{Character vector of additional negation markers.
Default \code{NULL}.}
}
\value{
The input tibble with an added logical column \code{negated}.
Tokens within the negation window are \code{TRUE}; all others are
\code{FALSE}.
}
\description{
Scans each document in a tokenised tibble for negation markers and flags
the next \code{window} tokens for polarity reversal. The output adds a
logical \code{negated} column used by \code{\link{mixology_sentiment}} to
invert matched token polarities during scoring. Ambiguous tokens are not
affected by negation.
}
\examples{
tweets <- c(
  "The vaccine is not effective.",
  "Lockdown measures are never justified."
)

toks <- mixology_tokenize(tweets)
mixology_negation(toks)
mixology_negation(toks, window = 5L)
mixology_negation(toks, custom_negations = c("hardly", "barely"))
}
\seealso{
\code{\link{mixology_tokenize}}, \code{\link{mixology_sentiment}}
}

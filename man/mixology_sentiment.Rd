% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{mixology_sentiment}
\alias{mixology_sentiment}
\title{Compute sentiment scores for a text corpus}
\usage{
mixology_sentiment(
  text,
  lexicon = "covid",
  weighted = TRUE,
  handle_negation = FALSE,
  negation_window = 3L,
  remove_stopwords = TRUE,
  custom_stopwords = NULL,
  custom_negations = NULL
)
}
\arguments{
\item{text}{Character vector. One element per document or tweet.}

\item{lexicon}{Character or data frame. Either a lexicon key string —
one of \code{"inquirer"}, \code{"subjectivity"}, \code{"bing"},
\code{"nrc"}, \code{"afinn"}, \code{"loughran"}, \code{"covid"},
\code{"mixology"}, \code{"covid_ft"}, or \code{"mixology_ft"} — or a
user-supplied data frame prepared with \code{\link{use_custom_lexicon}},
containing at minimum columns \code{word} and \code{sentiment}.
Default \code{"covid"}.}

\item{weighted}{Logical. If \code{TRUE} (default), corpus frequency weights
are applied for the \code{"covid"}, \code{"mixology"}, \code{"covid_ft"},
and \code{"mixology_ft"} lexicons. Has no effect for general-purpose
lexicons or user-supplied lexicons without a \code{weight} column.}

\item{handle_negation}{Logical. If \code{TRUE}, polarity reversal is applied
within a sliding window following negation markers. Default \code{FALSE}.}

\item{negation_window}{Integer. Width of the negation sliding window in
tokens. Default \code{3L}. Only used when \code{handle_negation = TRUE}.}

\item{remove_stopwords}{Logical. If \code{TRUE} (default), the bundled
domain-specific stop word list is applied before tokenisation.}

\item{custom_stopwords}{Character vector of additional stop words.
Default \code{NULL}.}

\item{custom_negations}{Character vector of additional negation markers.
Default \code{NULL}.}
}
\value{
A tibble with one row per input document and nine columns:
\describe{
  \item{doc_id}{Integer. Document position in the input vector.}
  \item{n_tokens}{Integer. Number of tokens after preprocessing.}
  \item{n_matched}{Numeric. Number of tokens matched to the lexicon.}
  \item{coverage}{Numeric. Proportion of tokens matched (0--1).}
  \item{score_positive}{Numeric. Aggregated positive sentiment score.}
  \item{score_negative}{Numeric. Aggregated negative sentiment score.}
  \item{score_ambiguous}{Numeric. Aggregated ambiguous sentiment score.}
  \item{score_net}{Numeric. Net sentiment score
    (\code{score_positive - score_negative}).}
  \item{polarity}{Character. Dominant polarity classification:
    \code{"positive"}, \code{"negative"}, \code{"ambiguous"}, or
    \code{"none"} (no tokens matched).}
}
}
\description{
Implements the full Mixology sentiment analysis pipeline: tokenise text,
optionally apply negation detection, match tokens against a specified
lexicon, and aggregate positive, negative, and ambiguous scores at the
document level. Accepts all ten built-in lexicons as well as
user-supplied data frames prepared with \code{\link{use_custom_lexicon}}.
}
\examples{
tweets <- c(
  "The lockdown is terrible and unjustified",
  "The vaccine rollout has been excellent",
  "I am not sure about the booster"
)

# Default: Mixology Covid lexicon with frequency weighting
mixology_sentiment(tweets)

# Fine-tuned lexicon
mixology_sentiment(tweets, lexicon = "covid_ft")

# General-purpose lexicon, no weighting
mixology_sentiment(tweets, lexicon = "bing", weighted = FALSE)

# User-supplied custom lexicon
my_lex <- use_custom_lexicon(
  data.frame(term = c("excellent","terrible"), polarity = c("positive","negative")),
  word_col = "term", sentiment_col = "polarity"
)
mixology_sentiment(tweets, lexicon = my_lex)

# Enable negation handling
mixology_sentiment(tweets, handle_negation = TRUE)
}
\seealso{
\code{\link{compare_lexicons}}, \code{\link{mixology_lexicon_names}},
\code{\link{get_lexicon}}, \code{\link{mixology_negation}},
\code{\link{use_custom_lexicon}}
}
